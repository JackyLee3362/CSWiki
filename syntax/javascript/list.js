var _ = require("lodash");
const nums = [1, 3, 5, 2, 4, 6];
// 初始化
const dp = new Array(n).fill(0);
// 判断为空
nums.length == 0
// 求长度
let n = nums.length;
// 添加数push
nums.push(5)
// 弹出
nums.pop()
// 数组求和
let sum_nums = _.sum(nums);
// 找到最后一个数
nums.at(-1)

